// lol I dont know funtions yet so here is a random one
function addTwoNumbers(num1, num2) {
    return num1 + num2;
}

// Example usage:
const result = addTwoNumbers(5, 10);
console.log(result); // Output: 15
// hope this helps you understand functions better!
// another function to multiply two numbers
function multiplyTwoNumbers(num1, num2) {
    return num1 * num2;
}

// Example usage:
const product = multiplyTwoNumbers(5, 10);
console.log(product); // Output: 50

// big line of code to test the limits of the function and see how it handles long lines of code without breaking or causing any issues in the execution of the program, which is important for maintaining readability and ensuring that the code remains clean and organized for future maintenance and updates.            
function longLineOfCode() {
    console.log("This is a very long line of code that is meant to test the limits of the function and see how it handles long lines of code without breaking or causing any issues in the execution of the program, which is important for maintaining readability and ensuring that the code remains clean and organized for future maintenance and updates.");
}

// Example usage:
longLineOfCode(); // Output: This is a very long line of code that is meant to test the limits of the function and see how it handles long lines of code without breaking or causing any issues in the execution of the program, which is important for maintaining readability and ensuring that the code remains clean and organized for future maintenance and updates.                      
let x = 10;
let y = 20;

function addAndMultiply(num1, num2) {
    const sum = num1 + num2;
    const product = num1 * num2;
    return { sum, product };
}

// Example usage:
const results = addAndMultiply(x, y);
console.log(results); // Output: { sum: 30, product: 200 }  

//   a function to calculate the area of a rectangle
function calculateRectangleArea(length, width) {
    return length * width;
}

// Example usage:
const area = calculateRectangleArea(5, 10);
console.log(area); // Output: 50          

